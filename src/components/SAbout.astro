<section id="about" class="s-about" data-intersect>
  <div class="s__inner js-inner">
    <div class="s__awards-wrapper">
      <!-- Top Pill Header -->
      <div class="s__awards-header">
        <span class="s__awards-header__tag">ABOUT & EXPERIENCE</span>
      </div>

      <!-- Bento Grid -->
      <div class="s__bento">
        <!-- Row 1: Motto & Role -->
        <div class="s__cell s__cell--full js-award">
          <div class="s__cell__content">
            <h3 class="s__cell__serif">"Being in a Black & White World, Making Things Colorful."</h3>
            <p class="s__cell__serif-sub">Creative Developer & UI/UX Specialist — Kolkata, India</p>
          </div>
          <span class="s__cell__mask"></span>
        </div>

        <!-- Row 2: Split 2 Column (4+ YRS EXP left, Minerva & AJIO right) -->
        <div class="s__row s__row--split">
          <!-- Left: 4+ YRS EXP tall box -->
          <div class="s__cell s__cell--awwwards js-award">
            <div class="s__cell__content">
              <div class="s__cell__huge">
                4+ YRS<br />EXP.
              </div>
              <div class="s__cell__meta">
                <span>(4+ Years Experience)</span>
                <span>(Digital Nomad)</span>
                <span>(Bespoke Dev & Art)</span>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>

          <!-- Right: Stacked Column -->
          <div class="s__col">
            <!-- Top: Minerva Infotech -->
            <div class="s__cell s__cell--netmag js-award">
              <div class="s__cell__content">
                <h4 class="s__cell__serif">Minerva Infotech</h4>
                <p class="s__cell__serif-sub">Creative Dev & Video (2024)</p>
              </div>
              <span class="s__cell__mask"></span>
            </div>

            <!-- Bottom: Hatching stripes + AJIO / Netscribes -->
            <div class="s__subrow">
              <div class="s__cell s__cell--stripes">
                <div class="s__stripes-pattern"></div>
              </div>

              <div class="s__cell s__cell--fwa js-award">
                <div class="s__cell__content">
                  <div class="s__cell__bold">AJIO</div>
                  <div class="s__cell__meta">
                    <span>@Netscribes India</span>
                    <span>(Contract 2024)</span>
                  </div>
                </div>
                <span class="s__cell__mask"></span>
              </div>
            </div>
          </div>
        </div>

        <!-- Row 3: World Mark Foundation & Freelance -->
        <div class="s__cell s__cell--full js-award">
          <div class="s__cell__content">
            <h3 class="s__cell__serif">World Mark Foundation & Freelance Nomad</h3>
            <p class="s__cell__serif-sub">UI/UX Architecture, Webflow & Motion Systems (2021 — Present)</p>
          </div>
          <span class="s__cell__mask"></span>
        </div>

        <!-- Row 3.5: Verified Client & Craft Metrics Bar -->
        <div class="s__cell s__cell--full s__cell--metrics js-award">
          <div class="s__crosshair s__crosshair--tl">+</div>
          <div class="s__crosshair s__crosshair--tr">+</div>
          <div class="s__cell__content">
            <div class="s__metrics-header">
              <span class="s__metrics-tag">[ VERIFIED IMPACT &amp; CLIENT DELIVERIES ]</span>
              <span class="s__metrics-live"><span class="s__metrics-pulse"></span>ACTIVE ROSTER 2026</span>
            </div>
            <div class="s__metrics-grid">
              <div class="s__metric-card">
                <strong class="s__metric-num">30+</strong>
                <span class="s__metric-label">Bespoke Platforms</span>
                <span class="s__metric-sub">Sub-0.5s Edge CDN</span>
              </div>
              <div class="s__metric-card">
                <strong class="s__metric-num">99.4%</strong>
                <span class="s__metric-label">Client Satisfaction</span>
                <span class="s__metric-sub">Milestone Sign-Off</span>
              </div>
              <div class="s__metric-card">
                <strong class="s__metric-num">15+</strong>
                <span class="s__metric-label">Global Timezones</span>
                <span class="s__metric-sub">US, UK, EU, UAE, IN</span>
              </div>
              <div class="s__metric-card">
                <strong class="s__metric-num">&lt;24h</strong>
                <span class="s__metric-label">Revision Turnaround</span>
                <span class="s__metric-sub">Direct WhatsApp Room</span>
              </div>
            </div>
          </div>
          <span class="s__cell__mask"></span>
        </div>

        <!-- Row 4: Bottom 3-cell Row (Globe + Skills + Marquee) -->
        <div class="s__row s__row--bottom">
          <!-- Left: Wireframe Globe with Crosshairs -->
          <div class="s__cell s__cell--globe js-award">
            <div class="s__crosshair s__crosshair--tl">+</div>
            <div class="s__crosshair s__crosshair--tr">+</div>
            <div class="s__cell__content">
              <svg class="s__globe-icon" width="68" height="68" viewBox="0 0 101 113" fill="none">
                <path
                  d="M80.05 107.398c-21.944 12.67-52.875.18-69.085-27.896m69.086 27.896c21.945-12.67 26.594-45.702 10.384-73.778M80.05 107.398c-19.686 11.366-48.786-2.181-64.996-30.258C-1.155 49.063 1.663 17.09 21.35 5.723m58.702 101.675c19.686-11.366 22.504-43.34 6.293-71.417C70.134 7.904 41.034-5.642 21.35 5.723m58.702 101.675c-10.973 6.335-33.009-11.29-49.219-39.367-16.21-28.077-20.456-55.973-9.483-62.308m58.702 101.675c10.972-6.335 6.726-34.231-9.484-62.308C54.357 17.013 32.322-.612 21.35 5.723m58.702 101.675c3.74-2.159-6.262-26.77-22.456-54.818S25.089 3.564 21.349 5.723m58.702 101.675c-3.74 2.159-20.053-18.808-36.246-46.856C27.61 32.493 17.609 7.882 21.349 5.723M10.965 79.502C-5.245 51.425-.596 18.393 21.349 5.723M10.965 79.502c11.366 19.686 38.37 25.373 60.314 12.703 21.946-12.67 30.522-38.9 19.156-58.585m-79.47 45.882C-.401 59.816 8.175 33.586 30.121 20.916c21.945-12.67 48.948-6.982 60.314 12.704m-79.47 45.882c6.335 10.972 29.26 9.596 51.206-3.074C84.115 63.758 96.77 44.592 90.434 33.62m-79.47 45.882C4.63 68.529 17.285 49.363 39.23 36.693c21.945-12.67 44.87-14.046 51.205-3.073M21.349 5.723c21.945-12.67 52.876-.18 69.086 27.897M54.749 63.573C32.778 76.258 13.192 83.358 11.033 79.618c-2.16-3.74 13.783-17.15 35.754-29.836 21.97-12.685 41.557-19.785 43.716-16.045 2.159 3.74-13.783 17.15-35.754 29.836Z"
                  stroke="currentColor"
                  stroke-width="1.2"
                />
              </svg>
            </div>
            <span class="s__cell__mask"></span>
          </div>

          <!-- Center: Core Skills -->
          <div class="s__cell s__cell--cssda js-award">
            <div class="s__cell__content">
              <div class="s__cell__bold">SKILLS</div>
              <div class="s__cell__meta">
                <span>(Next.js & React 19)</span>
                <span>(TypeScript & Astro)</span>
                <span>(Figma & UI/UX)</span>
                <span>(GSAP & Motion)</span>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>

          <!-- Right: Live Dual-Track Tech Marquee -->
          <div class="s__cell s__cell--tech-marquee js-award" title="Fullstack Creative Developer Stack">
            <div class="s__crosshair s__crosshair--tl">+</div>
            <div class="s__crosshair s__crosshair--tr">+</div>
            <div class="s__marquee-inner">
              <div class="s__marquee-header">
                <span class="s__marquee-pulse"></span>
                <span class="s__marquee-title">TECH RADAR</span>
              </div>
              <div class="s__marquee-track s__marquee-track--left">
                <div class="s__marquee-content">
                  <span>Next.js 15 &bull; React 19 &bull; TypeScript &bull; Astro 5 &bull; GSAP 3 &bull; Tailwind CSS &bull; Lenis &bull; SCSS &bull;&nbsp;</span>
                  <span>Next.js 15 &bull; React 19 &bull; TypeScript &bull; Astro 5 &bull; GSAP 3 &bull; Tailwind CSS &bull; Lenis &bull; SCSS &bull;&nbsp;</span>
                </div>
              </div>
              <div class="s__marquee-track s__marquee-track--right">
                <div class="s__marquee-content">
                  <span>Figma &bull; DaVinci Resolve &bull; Illustrator &bull; After Effects &bull; Vercel Edge &bull; Render &bull; Procreate &bull;&nbsp;</span>
                  <span>Figma &bull; DaVinci Resolve &bull; Illustrator &bull; After Effects &bull; Vercel Edge &bull; Render &bull; Procreate &bull;&nbsp;</span>
                </div>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>
        </div>

        <!-- Row 4.5: Creative Arsenal & Workstation Rig -->
        <div class="s__cell s__cell--full s__cell--arsenal js-award">
          <div class="s__crosshair s__crosshair--tl">+</div>
          <div class="s__crosshair s__crosshair--tr">+</div>
          <div class="s__cell__content">
            <div class="s__arsenal-header">
              <div class="s__arsenal-title-wrap">
                <span class="s__arsenal-tag">[ WORKSPACE ARSENAL &amp; PRODUCTION RIG ]</span>
                <span class="s__arsenal-badge">✦ PRO HARDWARE &amp; SOFTWARE</span>
              </div>
              <span class="s__arsenal-loc">📍 KOLKATA (UTC+5:30) &bull; GLOBAL CLIENTS</span>
            </div>

            <div class="s__arsenal-grid">
              <div class="s__arsenal-card">
                <div class="s__arsenal-icon">🖥️</div>
                <div class="s__arsenal-info">
                  <strong>Master Display</strong>
                  <p>M-Series Apple Silicon + 4K Color-Accurate Retina Rig</p>
                  <span class="s__arsenal-spec">Calibrated Viewports</span>
                </div>
              </div>

              <div class="s__arsenal-card">
                <div class="s__arsenal-icon">🎨</div>
                <div class="s__arsenal-info">
                  <strong>Design Studio</strong>
                  <p>Figma Enterprise + Canva Pro Master + Procreate</p>
                  <span class="s__arsenal-spec">Vector &amp; 300 DPI Ready</span>
                </div>
              </div>

              <div class="s__arsenal-card">
                <div class="s__arsenal-icon">⚡</div>
                <div class="s__arsenal-info">
                  <strong>Fullstack Lab</strong>
                  <p>Next.js 15 App Router + React 19 + Astro 5 + TypeScript</p>
                  <span class="s__arsenal-spec">Sub-Second Load Guaranteed</span>
                </div>
              </div>

              <div class="s__arsenal-card">
                <div class="s__arsenal-icon">🎬</div>
                <div class="s__arsenal-info">
                  <strong>Motion Systems</strong>
                  <p>GSAP 3 Shaders + Lenis Smooth Scroll + DaVinci Resolve</p>
                  <span class="s__arsenal-spec">60FPS Cinema Motion</span>
                </div>
              </div>
            </div>
          </div>
          <span class="s__cell__mask"></span>
        </div>

        <!-- Row 5: Verified Client Endorsements Bento Row -->
        <div class="s__row s__row--testimonials">
          <!-- Testimonial 1: Minerva Infotech -->
          <div class="s__cell s__cell--testimonial js-award">
            <div class="s__crosshair s__crosshair--tl">+</div>
            <div class="s__crosshair s__crosshair--tr">+</div>
            <div class="s__cell__content">
              <div class="s__t-header">
                <span class="s__t-tag">MINERVA INFOTECH</span>
                <span class="s__t-badge">✓ VERIFIED</span>
              </div>
              <p class="s__t-quote">
                "Brinta's attention to visual detail, motion timing, and frontend finesse elevated our client deliverables significantly."
              </p>
              <div class="s__t-author">
                <span class="s__t-name">Creative Director</span>
                <span class="s__t-sub">Web & Video Strategy</span>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>

          <!-- Testimonial 2: Netscribes (AJIO) -->
          <div class="s__cell s__cell--testimonial js-award">
            <div class="s__crosshair s__crosshair--tl">+</div>
            <div class="s__crosshair s__crosshair--tr">+</div>
            <div class="s__cell__content">
              <div class="s__t-header">
                <span class="s__t-tag">NETSCRIBES &bull; AJIO</span>
                <span class="s__t-badge">✓ VERIFIED</span>
              </div>
              <p class="s__t-quote">
                "Exceptional delivery speed across high-volume brand collateral while strictly preserving design integrity."
              </p>
              <div class="s__t-author">
                <span class="s__t-name">Design Lead</span>
                <span class="s__t-sub">Enterprise E-commerce</span>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>

          <!-- Testimonial 3: World Mark Foundation -->
          <div class="s__cell s__cell--testimonial js-award">
            <div class="s__crosshair s__crosshair--tl">+</div>
            <div class="s__crosshair s__crosshair--tr">+</div>
            <div class="s__cell__content">
              <div class="s__t-header">
                <span class="s__t-tag">WORLD MARK FDN.</span>
                <span class="s__t-badge">✓ VERIFIED</span>
              </div>
              <p class="s__t-quote">
                "Transformed our digital footprint into an accessible, responsive platform that drove record community trust."
              </p>
              <div class="s__t-author">
                <span class="s__t-name">Managing Director</span>
                <span class="s__t-sub">NGO & Social Impact</span>
              </div>
            </div>
            <span class="s__cell__mask"></span>
          </div>
        </div>

        <!-- Row 5.5: Visual Craft & Career Trajectory Timeline -->
        <div class="s__cell s__cell--full s__cell--timeline js-award">
          <div class="s__crosshair s__crosshair--tl">+</div>
          <div class="s__crosshair s__crosshair--tr">+</div>
          <div class="s__cell__content">
            <div class="s__timeline-header">
              <span class="s__timeline-tag">[ CAREER &amp; CRAFT TRAJECTORY ]</span>
              <span class="s__timeline-badge">● 2021 — 2026 // 4+ YEARS</span>
            </div>

            <div class="s__timeline-track">
              <div class="s__timeline-item">
                <div class="s__tl-year">2021</div>
                <div class="s__tl-dot"></div>
                <div class="s__tl-content">
                  <strong>Freelance Nomad Kickoff</strong>
                  <p>Webflow interfaces, responsive architectures &amp; custom digital anime art in Procreate.</p>
                </div>
              </div>

              <div class="s__timeline-item">
                <div class="s__tl-year">2022-23</div>
                <div class="s__tl-dot"></div>
                <div class="s__tl-content">
                  <strong>World Mark Foundation</strong>
                  <p>Lead UI/UX Architect &amp; Motion Lead. Transformed platforms for 100K+ community reach.</p>
                </div>
              </div>

              <div class="s__timeline-item">
                <div class="s__tl-year">2024</div>
                <div class="s__tl-dot"></div>
                <div class="s__tl-content">
                  <strong>AJIO &amp; Minerva Infotech</strong>
                  <p>High-volume enterprise e-commerce assets at Netscribes (AJIO) + 4K video motion systems.</p>
                </div>
              </div>

              <div class="s__timeline-item is-current">
                <div class="s__tl-year">2025-26</div>
                <div class="s__tl-dot is-active"></div>
                <div class="s__tl-content">
                  <strong>Creative Dev Specialist</strong>
                  <p>Bespoke Next.js 15, Astro 5, 3D Canvas, Canva Pro decks &amp; high-conversion web apps.</p>
                </div>
              </div>
            </div>
          </div>
          <span class="s__cell__mask"></span>
        </div>

        <!-- Row 6: Detailed Bio Narrative -->
        <div class="s__cell s__cell--full s__cell--bio js-award">
          <div class="s__cell__content">
            <p class="s__cell__bio-text">
              Immersed for 4+ years in creative development, responsive web design, visual branding, and motion graphics. From Netflix streaming clones and NGO platforms on Webflow & Wix Studio to custom anime sketch art in Procreate and high-impact social campaigns, turning complex ideas into captivating digital experiences.
            </p>
          </div>
          <span class="s__cell__mask"></span>
        </div>
      </div><!-- .s__bento -->
    </div><!-- .s__awards-wrapper -->
  </div><!-- .s__inner -->

  <svg class="s__grid js-grid">
    <path class="js-path" d=""></path>
  </svg>

  <canvas class="s__canvas js-canvas"></canvas>
</section>

<script>
  import Emitter from '../utils/Emitter'
  import Ticker from '../utils/Ticker'

  class Section {
    el: HTMLElement
    inner: HTMLElement
    svg: HTMLElement
    path: SVGPathElement
    canvas: HTMLCanvasElement
    ctx: CanvasRenderingContext2D | null
    awards: NodeListOf<HTMLElement>

    smileyImages: {
      main: HTMLImageElement
      contrasted: HTMLImageElement
    }

    bounding: {
      left: number
      top: number
      width: number
      height: number
      innerWidth: number
      innerHeight: number
      offsetY: number
    }

    scroll: {
      start: number
      end: number
      p: number
      sp: number
    }

    lines: any[]
    smileys: any[]

    isPaused: boolean
    isForced: boolean

    /**
     * Constructor
     */
    constructor() {
      // Elements
      this.el = document.querySelector('.s-about') as HTMLElement
      this.inner = this.el?.querySelector('.js-inner') as HTMLElement
      this.svg = this.el?.querySelector('.js-grid') as HTMLElement
      this.path = this.el?.querySelector('.js-path') as SVGPathElement
      this.canvas = document.querySelector('.js-canvas') as HTMLCanvasElement
      this.ctx = this.canvas?.getContext('2d')
      this.awards = this.el?.querySelectorAll('.js-award') as NodeListOf<HTMLElement>

      // Properties
      this.lines = []
      this.smileys = []

      this.isPaused = true
      this.isForced = false

      // Init
      if (document.readyState === 'complete') {
        Ticker.nextTick(this.init, this)
      } else {
        Emitter.once('siteLoaded', this.init, this)
      }
    }

    /**
     * Init
     */
    init() {
      this.createSmiley()

      this.setSize()
      this.setScroll()

      this.bindEvents()

      if (this.el?.classList.contains('is-in-view') && this.isPaused) {
        this.isPaused = false
        Emitter.on('tick', this.tick, this)
      }
    }

    /**
     * Bind events
     */
    bindEvents() {
      Emitter.on('resize', this.onResize, this)
      Emitter.on('scroll', this.onScroll, this)

      // Reveal on scroll
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-revealed')
              observer.unobserve(entry.target)
            }
          })
        },
        {
          threshold: 0.5,
        }
      )

      this.awards?.forEach((award) => {
        observer.observe(award)

        const awardInteraction = this.onAwardInteraction.bind(this, award)
        award.addEventListener('mouseenter', awardInteraction, {
          passive: true,
        })
        award.addEventListener('touchstart', awardInteraction, {
          passive: true,
        })
      })

      this.el?.addEventListener('intersect', this.onIntersect.bind(this), {
        passive: true,
      })
    }

    /**
     * Resize handler
     */
    onResize(widthChanged: boolean) {
      if (widthChanged) {
        this.setSize()
        this.setScroll()

        this.isForced = true
      }
    }

    /**
     * Scroll handler
     */
    onScroll(scrollY: number) {
      const { scroll } = this
      if (!scroll) return

      const trigger = scrollY + window.safeHeight

      if (trigger < scroll.start) {
        scroll.p = 0
      } else if (trigger > scroll.end) {
        scroll.p = 1
      } else {
        scroll.p = (trigger - scroll.start) / (scroll.end - scroll.start)
      }
    }

    /**
     * Intersect handler
     */
    onIntersect(e: any) {
      this.isPaused = !e.detail.isIntersecting

      if (this.isPaused) {
        Emitter.off('tick', this.tick, this)
      } else {
        Emitter.on('tick', this.tick, this)
      }
    }

    /**
     * Award interaction handler
     */
    onAwardInteraction(award: HTMLElement) {
      award.classList.add('is-active')

      this.throwSmileys(award)

      setTimeout(() => {
        award.classList.remove('is-active')
      }, 100)
    }

    /**
     * Create smiley
     */
    createSmiley() {
      const mainImg = new Image(100, 100)
      mainImg.src = '/images/asset-smiley--main.svg'

      const contrastedImg = new Image(100, 100)
      contrastedImg.src = '/images/asset-smiley--contrasted.svg'

      this.smileyImages = {
        main: mainImg,
        contrasted: contrastedImg,
      }
    }

    /**
     * Set size
     */
    setSize() {
      const { canvas, svg, el, inner } = this
      if (!el || !inner || !svg || !canvas) return

      const bounding = el.getBoundingClientRect()

      this.bounding = {
        left: bounding.left,
        top: bounding.top,
        width: el.clientWidth,
        height: el.clientHeight,
        innerWidth: inner.clientWidth,
        innerHeight: inner.clientHeight,
        offsetY: 0,
      }

      svg.style.width = `${this.bounding.width}px`
      svg.style.height = `${this.bounding.height}px`

      canvas.width = this.bounding.width
      canvas.height = this.bounding.height
    }

    /**
     * Set scroll
     */
    setScroll() {
      const { bounding } = this
      if (!bounding) return

      this.scroll = {
        start: bounding.top + window.scrollY,
        end:
          bounding.top + window.scrollY + bounding.height + window.safeHeight,
        p: 0,
        sp: 0,
      }

      this.onScroll(window.scrollY)
      this.scroll.sp = this.scroll.p
    }

    /**
     * Set lines
     */
    setLines() {
      const { bounding } = this
      if (!bounding) return

      this.lines = []

      const innerX = (bounding.width - bounding.innerWidth) / 2
      const innerY =
        (bounding.height - bounding.innerHeight) / 2 + bounding.offsetY

      const vLines = window.safeWidth > 767 ? 12 : 8
      const hLines = 4

      const outerGapX = bounding.width / vLines
      const outerGapY = bounding.height / vLines
      const innerGapX = bounding.innerWidth / vLines
      const innerGapY = bounding.innerHeight / vLines
      const hGap = 1 / hLines

      const outer = {
        x1: 0,
        x2: bounding.width,
        y1: 0,
        y2: bounding.height,
      }
      const inner = {
        x1: innerX,
        x2: innerX + bounding.innerWidth,
        y1: innerY,
        y2: innerY + bounding.innerHeight,
      }
      const corners: any[] = []

      // Top left
      corners.push([
        { x: outer.x1, y: outer.y1 },
        { x: inner.x1, y: inner.y1 },
      ])

      // Top right
      corners.push([
        { x: outer.x2, y: outer.y1 },
        { x: inner.x2, y: inner.y1 },
      ])

      // Bottom right
      corners.push([
        { x: outer.x2, y: outer.y2 },
        { x: inner.x2, y: inner.y2 },
      ])

      // Bottom left
      corners.push([
        { x: outer.x1, y: outer.y2 },
        { x: inner.x1, y: inner.y2 },
      ])

      for (let i = 1; i < vLines; i++) {
        this.lines.push([
          { x: outerGapX * i, y: outer.y1 },
          { x: innerX + innerGapX * i, y: inner.y1 },
        ])

        this.lines.push([
          { x: outerGapX * i, y: outer.y2 },
          { x: innerX + innerGapX * i, y: inner.y2 },
        ])
      }

      for (let i = 1; i < hLines; i++) {
        const index = 1 - Math.pow(1 - hGap * i, 2)
        const l1 = corners[0]
        const l2 = corners[1]

        const p1 = {
          x: l1[0].x + (l1[1].x - l1[0].x) * index,
          y: l1[0].y + (l1[1].y - l1[0].y) * index,
        }
        const p2 = {
          x: l2[0].x + (l2[1].x - l2[0].x) * index,
          y: l2[0].y + (l2[1].y - l2[0].y) * index,
        }
        this.lines.push([p1, p2])
      }

      for (let i = 1; i < hLines; i++) {
        const index = 1 - Math.pow(1 - hGap * i, 2)
        const l1 = corners[3]
        const l2 = corners[2]

        const p1 = {
          x: l1[0].x + (l1[1].x - l1[0].x) * index,
          y: l1[0].y + (l1[1].y - l1[0].y) * index,
        }
        const p2 = {
          x: l2[0].x + (l2[1].x - l2[0].x) * index,
          y: l2[0].y + (l2[1].y - l2[0].y) * index,
        }
        this.lines.push([p1, p2])
      }

      for (let i = 0; i <= vLines; i++) {
        this.lines.push([
          { x: outer.x1, y: outerGapY * i },
          { x: inner.x1, y: innerY + innerGapY * i },
        ])

        this.lines.push([
          { x: outer.x2, y: outerGapY * i },
          { x: inner.x2, y: innerY + innerGapY * i },
        ])
      }

      for (let i = 1; i < hLines; i++) {
        const index = 1 - Math.pow(1 - hGap * i, 2)
        const l1 = corners[1]
        const l2 = corners[2]

        const p1 = {
          x: l1[0].x + (l1[1].x - l1[0].x) * index,
          y: l1[0].y + (l1[1].y - l1[0].y) * index,
        }
        const p2 = {
          x: l2[0].x + (l2[1].x - l2[0].x) * index,
          y: l2[0].y + (l2[1].y - l2[0].y) * index,
        }
        this.lines.push([p1, p2])
      }

      for (let i = 1; i < hLines; i++) {
        const index = 1 - Math.pow(1 - hGap * i, 2)
        const l1 = corners[0]
        const l2 = corners[3]

        const p1 = {
          x: l1[0].x + (l1[1].x - l1[0].x) * index,
          y: l1[0].y + (l1[1].y - l1[0].y) * index,
        }
        const p2 = {
          x: l2[0].x + (l2[1].x - l2[0].x) * index,
          y: l2[0].y + (l2[1].y - l2[0].y) * index,
        }
        this.lines.push([p1, p2])
      }
    }

    /**
     * Draw lines
     */
    drawLines() {
      let d = ''
      this.lines.forEach((line) => {
        d += `M ${Math.round(line[0].x)} ${Math.round(line[0].y)} L ${Math.round(line[1].x)} ${Math.round(line[1].y)} `
      })
      if (this.path) {
        this.path.setAttribute('d', d)
      }
    }

    /**
     * Throw smileys
     */
    throwSmileys(award: HTMLElement) {
      const awardBounding = award.getBoundingClientRect()
      const total = 10

      for (let i = 0; i < total; i++) {
        const size = Math.random() * 20 + 20
        const x =
          awardBounding.left +
          Math.random() * awardBounding.width -
          this.bounding.left
        const y =
          awardBounding.top +
          Math.random() * awardBounding.height -
          this.bounding.top

        const velocity = {
          x: (Math.random() - 0.5) * 15,
          y: (Math.random() - 0.5) * 15 - 5,
        }

        this.smileys.push({
          x,
          y,
          size,
          velocity,
          gravity: 0.35,
          rotation: Math.random() * Math.PI * 2,
          rotationSpeed: (Math.random() - 0.5) * 0.2,
          opacity: 1,
        })
      }
    }

    /**
     * Update smileys
     */
    updateSmileys() {
      if (!this.ctx) return

      this.ctx.clearRect(0, 0, this.bounding.width, this.bounding.height)

      const isContrasted =
        document.documentElement.classList.contains('theme-contrasted')
      const img = isContrasted
        ? this.smileyImages.contrasted
        : this.smileyImages.main

      this.smileys.forEach((smiley, index) => {
        smiley.x += smiley.velocity.x
        smiley.y += smiley.velocity.y
        smiley.velocity.y += smiley.gravity
        smiley.rotation += smiley.rotationSpeed
        smiley.opacity -= 0.015

        if (smiley.opacity <= 0) {
          this.smileys.splice(index, 1)
          return
        }

        this.ctx!.save()
        this.ctx!.globalAlpha = Math.max(0, smiley.opacity)
        this.ctx!.translate(smiley.x, smiley.y)
        this.ctx!.rotate(smiley.rotation)
        this.ctx!.drawImage(
          img,
          -smiley.size / 2,
          -smiley.size / 2,
          smiley.size,
          smiley.size
        )
        this.ctx!.restore()
      })
    }

    /**
     * Tick
     */
    tick() {
      const { scroll } = this
      if (!scroll) return

      const spDelta = Math.abs(scroll.p - scroll.sp)

      if (spDelta > 0.001 || this.isForced) {
        scroll.sp += (scroll.p - scroll.sp) * 0.1
        this.bounding.offsetY = (scroll.sp - 0.5) * -150

        this.setLines()
        this.drawLines()

        this.isForced = false
      }

      if (this.smileys.length > 0) {
        this.updateSmileys()
      }
    }
  }

  new Section()
</script>

<style lang="scss">
  .s-about {
    position: relative;
    z-index: 3;

    padding: 7rem 0 8rem;

    background: color(primary);
    border-bottom: 1px solid color(secondary);
    overflow: hidden;

    @include mq(tablet) {
      padding: 4rem 0 5rem;
    }

    @include mq(phone) {
      padding: 2.25rem 0 3.5rem;
    }

    .s__inner {
      position: relative;
      z-index: 2;

      margin: 0 auto;
      width: 38rem;
      max-width: calc(100% - 2.5rem);

      @include mq(tablet) {
        width: min(46rem, calc(100% - 2.5rem));
      }

      @include mq(phone) {
        width: 100%;
        max-width: calc(100% - 1.5rem);
      }
    }

    .s__awards-wrapper {
      position: relative;
      width: 100%;
    }

    .s__awards-header {
      display: block;
      background: color(secondary);
      color: color(primary);
      text-align: center;
      padding: 0.45rem 1rem;
      border: 1px solid color(secondary);
      border-bottom: 0;

      &__tag {
        font: 700 0.6875rem / 1 font-family(fraktion);
        letter-spacing: 0.22em;
        text-transform: uppercase;
      }
    }

    .s__bento {
      display: flex;
      flex-direction: column;
      width: 100%;
    }

    .s__row {
      &--split {
        display: grid;
        grid-template-columns: 1.15fr 1fr;
        margin-top: -1px;

        > .s__cell {
          margin-top: 0;
        }

        > .s__col {
          display: flex;
          flex-direction: column;
          margin-left: -1px;
        }

        @include mq(phone) {
          grid-template-columns: 1fr;

          > .s__col {
            margin-left: 0;
            margin-top: -1px;
          }
        }
      }

      &--bottom {
        display: grid;
        grid-template-columns: 1fr 1.35fr 1fr;
        margin-top: -1px;

        > .s__cell {
          margin-top: 0;
        }

        > .s__cell:not(:first-child) {
          margin-left: -1px;
        }

        @include mq(phone) {
          grid-template-columns: 1fr;

          > .s__cell:not(:first-child) {
            margin-left: 0;
            margin-top: -1px;
          }
        }
      }

      &--testimonials {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        margin-top: -1px;

        > .s__cell {
          margin-top: 0;
        }

        > .s__cell:not(:first-child) {
          margin-left: -1px;
        }

        @include mq(tablet) {
          grid-template-columns: 1fr;

          > .s__cell:not(:first-child) {
            margin-left: 0;
            margin-top: -1px;
          }
        }
      }
    }

    .s__subrow {
      display: grid;
      grid-template-columns: 1fr 1fr;
      margin-top: -1px;
      flex: 1;

      > .s__cell {
        margin-top: 0;
      }

      > .s__cell:last-child {
        margin-left: -1px;
      }
    }

    .s__cell {
      position: relative;
      border: 1px solid color(secondary);
      margin-top: -1px;
      background: color(primary);
      overflow: hidden;
      cursor: pointer;

      &__content {
        position: relative;
        z-index: 2;
        color: color(secondary);
        transition: color 0.25s ease;
      }

      &__mask {
        position: absolute;
        top: 0;
        left: 0;
        z-index: 1;

        width: 100%;
        height: 100%;

        background: color(secondary);
        scale: 0 1;
        transform-origin: 0 50%;

        transition: scale 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        pointer-events: none;
      }

      @media (hover: hover) {
        &:hover {
          .s__cell__mask {
            scale: 1 1;
          }

          .s__cell__content {
            color: color(primary);
          }

          svg path {
            stroke: color(primary);
          }

          .s__crosshair {
            color: color(primary);
          }
        }
      }

      &:active {
        transform: scale(0.99);
      }

      &--full {
        padding: 2.25rem 1.5rem;
        text-align: center;
      }

      &--metrics {
        padding: 1.75rem 1.5rem;
        text-align: left;

        @include mq(phone) {
          padding: 1.25rem 1rem;
        }

        .s__metrics-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          flex-wrap: wrap;
          gap: 0.5rem;
          margin-bottom: 1.25rem;
        }

        .s__metrics-tag {
          font: 700 0.6875rem / 1.3 font-family(fraktion);
          letter-spacing: 0.16em;
          text-transform: uppercase;
          opacity: 0.85;
        }

        .s__metrics-live {
          display: inline-flex;
          align-items: center;
          gap: 0.35rem;
          font: 700 0.5625rem / 1 font-family(fraktion);
          letter-spacing: 0.12em;
          color: #10b981;
          background: rgba(16, 185, 129, 0.1);
          padding: 0.2rem 0.45rem;
          border-radius: 999px;
        }

        .s__metrics-pulse {
          width: 5px;
          height: 5px;
          border-radius: 50%;
          background: #10b981;
          animation: pulse 1.8s infinite;
        }

        .s__metrics-grid {
          display: grid;
          grid-template-columns: repeat(4, 1fr);
          gap: 0.85rem;

          @include mq(tablet) {
            grid-template-columns: repeat(2, 1fr);
          }

          @include mq(phone) {
            grid-template-columns: 1fr;
          }
        }

        .s__metric-card {
          padding: 1rem 0.85rem;
          background: rgba(0, 0, 0, 0.03);
          border: 1px solid rgba(0, 0, 0, 0.1);
          border-radius: 6px;
          display: flex;
          flex-direction: column;
          gap: 0.25rem;
          transition: transform 0.25s ease, border-color 0.25s ease;

          .s__metric-num {
            font: 700 2.25rem / 0.95 font-family(bigger);
            letter-spacing: -0.02em;
            color: color(secondary);
          }

          .s__metric-label {
            font: 700 0.625rem / 1.1 font-family(fraktion);
            letter-spacing: 0.08em;
            text-transform: uppercase;
            opacity: 0.9;
          }

          .s__metric-sub {
            font: 400 0.5625rem / 1.2 font-family(editorial);
            font-style: italic;
            opacity: 0.7;
          }
        }

        &:hover {
          .s__metric-card {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.15);

            .s__metric-num {
              color: #e8c547;
            }
          }
        }
      }

      &--arsenal {
        padding: 1.75rem 1.5rem;
        text-align: left;

        @include mq(phone) {
          padding: 1.25rem 1rem;
        }

        .s__arsenal-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          flex-wrap: wrap;
          gap: 0.5rem;
          margin-bottom: 1.25rem;
        }

        .s__arsenal-title-wrap {
          display: flex;
          align-items: center;
          gap: 0.65rem;
          flex-wrap: wrap;
        }

        .s__arsenal-tag {
          font: 700 0.6875rem / 1.3 font-family(fraktion);
          letter-spacing: 0.16em;
          text-transform: uppercase;
          opacity: 0.85;
        }

        .s__arsenal-badge {
          font: 700 0.5625rem / 1 font-family(fraktion);
          letter-spacing: 0.1em;
          padding: 0.2rem 0.45rem;
          background: rgba(0, 0, 0, 0.05);
          border-radius: 2px;
        }

        .s__arsenal-loc {
          font: 700 0.5625rem / 1 font-family(fraktion);
          letter-spacing: 0.08em;
          opacity: 0.75;
        }

        .s__arsenal-grid {
          display: grid;
          grid-template-columns: repeat(4, 1fr);
          gap: 0.85rem;

          @include mq(tablet) {
            grid-template-columns: repeat(2, 1fr);
          }

          @include mq(phone) {
            grid-template-columns: 1fr;
          }
        }

        .s__arsenal-card {
          padding: 1rem 0.85rem;
          background: rgba(0, 0, 0, 0.03);
          border: 1px solid rgba(0, 0, 0, 0.1);
          border-radius: 6px;
          display: flex;
          flex-direction: column;
          gap: 0.65rem;
          transition: transform 0.25s ease;

          .s__arsenal-icon {
            font-size: 1.5rem;
          }

          .s__arsenal-info {
            display: flex;
            flex-direction: column;
            gap: 0.3rem;

            strong {
              font: 700 0.8125rem / 1 font-family(fraktion);
              letter-spacing: 0.04em;
            }

            p {
              font: 400 0.6875rem / 1.35 font-family(editorial);
              opacity: 0.85;
              margin: 0;
            }

            .s__arsenal-spec {
              align-self: flex-start;
              font: 700 0.5rem / 1 font-family(fraktion);
              letter-spacing: 0.1em;
              text-transform: uppercase;
              padding: 0.15rem 0.35rem;
              background: rgba(0, 0, 0, 0.06);
              border-radius: 2px;
              margin-top: 0.25rem;
            }
          }
        }

        &:hover {
          .s__arsenal-badge {
            background: rgba(232, 197, 71, 0.2);
            color: #e8c547;
          }

          .s__arsenal-card {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.15);

            .s__arsenal-spec {
              background: rgba(232, 197, 71, 0.15);
              color: #e8c547;
            }
          }
        }
      }

      &--timeline {
        padding: 1.75rem 1.5rem;
        text-align: left;

        @include mq(phone) {
          padding: 1.25rem 1rem;
        }

        .s__timeline-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          flex-wrap: wrap;
          gap: 0.5rem;
          margin-bottom: 1.5rem;
        }

        .s__timeline-tag {
          font: 700 0.6875rem / 1.3 font-family(fraktion);
          letter-spacing: 0.16em;
          text-transform: uppercase;
          opacity: 0.85;
        }

        .s__timeline-badge {
          font: 700 0.5625rem / 1 font-family(fraktion);
          letter-spacing: 0.1em;
          padding: 0.2rem 0.45rem;
          background: rgba(0, 0, 0, 0.05);
          border-radius: 2px;
        }

        .s__timeline-track {
          display: grid;
          grid-template-columns: repeat(4, 1fr);
          gap: 1rem;
          position: relative;

          @include mq(tablet) {
            grid-template-columns: repeat(2, 1fr);
            gap: 1.25rem 1rem;
          }

          @include mq(phone) {
            grid-template-columns: 1fr;
          }
        }

        .s__timeline-item {
          display: flex;
          flex-direction: column;
          gap: 0.5rem;
          position: relative;
          padding-left: 0.25rem;

          .s__tl-year {
            font: 700 0.75rem / 1 font-family(fraktion);
            letter-spacing: 0.08em;
            color: color(secondary);
            opacity: 0.9;
          }

          .s__tl-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: color(secondary);
            opacity: 0.4;
            transition: all 0.25s ease;

            &.is-active {
              background: #10b981;
              opacity: 1;
              box-shadow: 0 0 8px #10b981;
            }
          }

          .s__tl-content {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;

            strong {
              font: 700 0.75rem / 1.2 font-family(fraktion);
              letter-spacing: 0.02em;
            }

            p {
              font: 400 0.6875rem / 1.4 font-family(editorial);
              opacity: 0.85;
              margin: 0;
            }
          }

          &.is-current {
            .s__tl-year {
              color: #10b981;
            }
          }
        }

        &:hover {
          .s__timeline-badge {
            background: rgba(232, 197, 71, 0.2);
            color: #e8c547;
          }

          .s__timeline-item {
            .s__tl-year {
              color: color(primary);
            }

            .s__tl-dot:not(.is-active) {
              background: color(primary);
              opacity: 0.6;
            }

            &.is-current .s__tl-year {
              color: #10b981;
            }
          }
        }
      }

      &--awwwards {
        padding: 1.75rem 1.5rem;

        .s__cell__content {
          height: 100%;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
        }
      }

      &--netmag {
        padding: 1.75rem 1.25rem;
        text-align: center;

        .s__cell__serif {
          font-size: 1.75rem;
        }

        .s__cell__serif-sub {
          font-size: 1.6rem;
        }
      }

      &--fwa {
        padding: 1.25rem 1rem;

        .s__cell__content {
          height: 100%;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
        }
      }

      &--cssda {
        padding: 1.25rem 1rem;
        text-align: center;

        .s__cell__content {
          height: 100%;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
        }
      }

      &--globe {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.25rem;
        min-height: 7.5rem;

        .s__globe-icon {
          display: block;
          margin: 0 auto;

          path {
            stroke: color(secondary);
            transition: stroke 0.25s ease;
          }
        }
      }

      &--tech-marquee {
        position: relative;
        padding: 0.85rem 0.75rem;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        justify-content: center;
        background: color(primary);
        min-height: 7.5rem;

        .s__marquee-inner {
          position: relative;
          z-index: 1;
          display: flex;
          flex-direction: column;
          gap: 0.45rem;
          width: 100%;
          overflow: hidden;
        }

        .s__marquee-header {
          display: flex;
          align-items: center;
          gap: 0.35rem;
          margin-bottom: 0.15rem;

          .s__marquee-pulse {
            width: 6px;
            height: 6px;
            background: #4ade80;
            border-radius: 50%;
            box-shadow: 0 0 6px #4ade80;
            animation: radarPulse 2s infinite ease-in-out;
          }

          .s__marquee-title {
            font: 700 9px / 1 font-family(fraktion);
            letter-spacing: 0.14em;
            color: color(secondary);
            opacity: 0.85;
            text-transform: uppercase;
          }
        }

        .s__marquee-track {
          display: flex;
          width: 100%;
          overflow: hidden;
          white-space: nowrap;

          .s__marquee-content {
            display: flex;
            white-space: nowrap;
            font: 600 11px / 1.2 font-family(fraktion);
            letter-spacing: 0.04em;
            color: color(secondary);
          }

          &--left .s__marquee-content {
            animation: marqueeLeft 18s linear infinite;
          }

          &--right .s__marquee-content {
            animation: marqueeRight 22s linear infinite;
          }
        }

        &:hover {
          .s__marquee-track--left .s__marquee-content,
          .s__marquee-track--right .s__marquee-content {
            animation-play-state: paused;
          }

          .s__marquee-header .s__marquee-title {
            color: color(primary);
          }

          .s__marquee-track .s__marquee-content {
            color: color(primary);
          }
        }
      }

      @keyframes marqueeLeft {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
      }

      @keyframes marqueeRight {
        0% { transform: translateX(-50%); }
        100% { transform: translateX(0); }
      }

      @keyframes radarPulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.3; transform: scale(0.8); }
      }

      &--stripes {
        background: repeating-linear-gradient(
          45deg,
          transparent,
          transparent 5px,
          color(secondary) 5px,
          color(secondary) 6.2px
        );
        cursor: default;
        min-height: 5.5rem;
      }

      &--testimonial {
        padding: 1.5rem 1.25rem;
        display: flex;
        flex-direction: column;

        .s__cell__content {
          height: 100%;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
        }

        .s__t-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 0.5rem;
          margin-bottom: 0.85rem;
        }

        .s__t-tag {
          font: 700 0.625rem / 1 font-family(fraktion);
          letter-spacing: 0.12em;
          text-transform: uppercase;
        }

        .s__t-badge {
          font: 700 0.5625rem / 1 font-family(fraktion);
          letter-spacing: 0.1em;
          text-transform: uppercase;
          background: color(secondary);
          color: color(primary);
          padding: 0.2rem 0.4rem;
          border-radius: 2px;
          transition: background 0.25s ease, color 0.25s ease;
        }

        .s__t-quote {
          font: 300 0.95rem / 1.45 font-family(editorial);
          letter-spacing: 0.01em;
          margin: 0 0 1rem;
          font-style: italic;
          opacity: 0.95;
        }

        .s__t-author {
          display: flex;
          flex-direction: column;
          gap: 0.15rem;
          border-top: 1px dashed color(secondary);
          padding-top: 0.65rem;
          transition: border-color 0.25s ease;
        }

        .s__t-name {
          font: 700 0.6875rem / 1 font-family(fraktion);
          letter-spacing: 0.06em;
          text-transform: uppercase;
        }

        .s__t-sub {
          font: 400 0.625rem / 1 font-family(fraktion);
          letter-spacing: 0.04em;
          opacity: 0.8;
          text-transform: uppercase;
        }

        &:hover {
          .s__t-badge {
            background: color(primary);
            color: color(secondary);
          }
          .s__t-author {
            border-top-color: color(primary);
          }
        }
      }

      &--bio {
        padding: 1.75rem 1.5rem;
        text-align: left;
      }

      &__bio-text {
        font: 300 1.15rem / 1.6 font-family(editorial);
        letter-spacing: 0.01em;
        margin: 0;
        opacity: 0.95;

        @include mq(phone) {
          font-size: 1rem;
          line-height: 1.5;
        }
      }

      &__serif {
        font: 300 2.15rem / 1.18 font-family(editorial);
        letter-spacing: -0.01em;
        margin: 0;

        @include mq(phone) {
          font-size: 1.5rem;
        }
      }

      &__serif-sub {
        font: italic 300 1.95rem / 1.18 font-family(editorial);
        letter-spacing: -0.01em;
        margin: 0.25rem 0 0;

        @include mq(phone) {
          font-size: 1.35rem;
        }
      }

      &__huge {
        font: 700 4.25rem / 0.82 font-family(bigger);
        text-transform: uppercase;
        letter-spacing: -0.02em;

        @include mq(phone) {
          font-size: 3.2rem;
        }
      }

      &__bold {
        font: 700 2.85rem / 0.85 font-family(bigger);
        text-transform: uppercase;
        letter-spacing: -0.02em;

        @include mq(phone) {
          font-size: 2.2rem;
        }
      }

      &__meta {
        font: 400 0.6875rem / 1.35 font-family(fraktion);
        letter-spacing: 0.06em;
        text-transform: uppercase;
        margin-top: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: 0.15rem;
        opacity: 0.9;
      }
    }

    .s__crosshair {
      position: absolute;
      font: 400 12px / 1 font-family(fraktion);
      color: color(secondary);
      z-index: 2;
      transition: color 0.25s ease;

      &--tl {
        top: 6px;
        left: 8px;
      }

      &--tr {
        top: 6px;
        right: 8px;
      }
    }

    .s__grid {
      position: absolute;
      top: 0;
      left: 0;
      z-index: 1;

      pointer-events: none;

      path {
        fill: none;
        stroke: color(secondary);
        stroke-width: 0.5px;
        opacity: 0.35;
      }
    }

    .s__canvas {
      position: absolute;
      top: 0;
      left: 0;
      z-index: 4;

      width: 100%;
      height: 100%;

      pointer-events: none;
    }
  }
</style>
