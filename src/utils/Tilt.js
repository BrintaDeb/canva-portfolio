/**
 * Tilt.js — Ultra-lightweight 3D Card Perspective Tilt & Dynamic Specular Sheen
 * (Disabled per user request to remove tilt effects across all sections)
 */

export class CardTilt {
  constructor() {}
  init() {}
  destroy() {}
}

export default CardTilt

